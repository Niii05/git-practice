name: Fail Test

on:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Start"

      - name: Break it
        run: exit 1

      - name: Step 3
        run: echo "This should not run"
