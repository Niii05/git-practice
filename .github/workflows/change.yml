name: Practice CI

on:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Start"

      - name: Step 2
        run: exit 1

      - name: Step 3
        run: echo "Should not run"
